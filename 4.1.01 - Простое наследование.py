'''Задача 1: Простое наследование
Условие:
Ваша первая задача на наследование — создать дочерний класс, который ничего не
добавляет к родительскому, но уже обладает всей его функциональностью.

Вам нужно:

Создать родительский класс Appliance (Бытовой прибор).

В __init__ он должен принимать model (модель) и сохранять в self.model. Также в
 __init__ должен быть атрибут self.is_on = False.

В классе Appliance должен быть метод turn_on(), который меняет self.is_on на True.

Создать дочерний класс Toaster, который наследует от Appliance.

Тело класса Toaster должно быть пустым (pass).

Шаблон кода автоматически создаст экземпляр Toaster и проверит, унаследовал ли
 он методы и атрибуты от Appliance.'''

class Appliance:
    # Конструктор класса Appliance: принимает модель прибора
    # и устанавливает начальное состояние — прибор выключен
    def __init__(self, model):
        self.model = model      # Сохраняем модель прибора
        self.is_on = False      # По умолчанию прибор выключен

    # Метод включает прибор, меняя состояние is_on на True
    def turn_on(self):
        self.is_on = True

# Класс Toaster наследует все свойства и методы от Appliance
# Пока он пустой (pass), так как не добавляет новой функциональности
class Toaster(Appliance):
    pass