'''Задача 4: Сеттер с валидацией
Условие:
Это ключевая задача на инкапсуляцию. Вам нужно создать метод,
который защищает атрибут от некорректных значений.

Вам нужно:

Создать класс User.

В __init__ создать защищенный атрибут self._age со значением по умолчанию 0.

Создать геттер get_age() для чтения self._age.

Создать публичный метод-сеттер set_age(new_age). Этот метод должен:

Проверять, является ли new_age числом и больше ли оно или равно 0.

Если проверка пройдена, изменить self._age на new_age.

Если проверка не пройдена, ничего не делать (оставить self._age без изменений).'''

class User:
    def __init__(self, age=0):
        self._age = age

    def get_age(self):
        return self._age

    def set_age(self, new_age):
        # Проверяем, является ли new_age числом (int или float) и >= 0
        if isinstance(new_age, (int, float)) and new_age >= 0:
            self._age = new_age
        # Если проверка не пройдена — ничего не делаем