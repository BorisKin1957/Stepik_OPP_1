'''
Задача 1: Простое использование isinstance()

Условие:
Ваша задача — написать функцию, которая определяет, является ли переданный
объект "домашним животным" (кошкой или собакой).

Вам нужно:

    Создать три класса: Animal, Cat(Animal) и Dog(Animal). Тела классов
    могут быть пустыми (pass).

    Написать отдельную функцию is_pet(animal_object), которая:

        Принимает на вход один объект animal_object.

        Возвращает True, если объект является экземпляром класса Cat или Dog.

        Возвращает False во всех остальных случаях.

        Используйте isinstance() для проверки.
'''

'''
Задача 1: Простое использование isinstance()

Условие:
Ваша задача — написать функцию, которая определяет, является ли переданный 
объект "домашним животным" (кошкой или собакой).

Вам нужно:

    Создать три класса: Animal, Cat(Animal) и Dog(Animal). Тела классов 
    могут быть пустыми (pass).

    Написать отдельную функцию is_pet(animal_object), которая:

        Принимает на вход один объект animal_object.

        Возвращает True, если объект является экземпляром класса Cat или Dog.

        Возвращает False во всех остальных случаях.

        Используйте isinstance() для проверки.
'''


# Базовый класс для всех животных
class Animal:
    pass  # Пока не содержит атрибутов или методов


# Класс кошки, наследуется от Animal
class Dog(Animal):
    pass  # Пока не содержит атрибутов или методов


# Класс собаки, наследуется от Animal
class Cat(Animal):
    pass  # Пока не содержит атрибутов или методов


# Функция проверяет, является ли объект домашним животным (кошкой или собакой)
def is_pet(animal_object):
    # Используем isinstance() для проверки принадлежности к классам Cat или Dog
    # Передаём кортеж классов: если объект — экземпляр любого из них, вернётся True
    return isinstance(animal_object, (Dog, Cat))


'''Пояснение:

isinstance(obj, (Class1, Class2)) — удобный способ проверить, является ли 
объект экземпляром одного из нескольких классов.
Поскольку Cat и Dog наследуются от Animal, но мы хотим отбирать только 
конкретно этих двух, используется проверка именно по ним.
Функция возвращает True только для кошек и собак, даже если есть другие 
наследники Animal.'''
