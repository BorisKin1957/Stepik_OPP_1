'''
Задача 5: Практический пример "Экспорт данных"

Условие:
Создайте простую систему для "экспорта" данных в разные форматы.

Вам нужно:

    Создать два класса-экспортера: JsonExporter и CsvExporter.

    В обоих классах должен быть метод export(data), который возвращает строку:

        JsonExporter должен возвращать "Экспорт в JSON: [data]".

        CsvExporter должен возвращать "Экспорт в CSV: [data]".

    Написать полиморфную функцию export_data(exporter, data), которая принимает
    объект-экспортер и данные, а затем вызывает у экспортера метод export и
    возвращает его результат.
'''


class JsonExporter:
    """Класс для экспорта данных в формат JSON."""
    def export(self, data):
        """
        Экспортирует переданные данные, возвращая строку с префиксом JSON.

        :param data: данные для экспорта (любой тип, преобразуется в строку)
        :return: строка с результатом экспорта
        """
        return f'Экспорт в JSON: {data}'


class CsvExporter:
    """Класс для экспорта данных в формат CSV."""
    def export(self, data):
        """
        Экспортирует переданные данные, возвращая строку с префиксом CSV.

        :param data: данные для экспорта (любой тип, преобразуется в строку)
        :return: строка с результатом экспорта
        """
        return f'Экспорт в CSV: {data}'


def export_data(exporter, data):
    """
    Универсальная функция экспорта данных.
    Принимает объект экспортера и данные, вызывает метод export.

    :param exporter: объект с методом export()
    :param data: данные для экспорта
    :return: результат вызова exporter.export(data)
    """
    return exporter.export(data)


# Примеры вызова кода
if __name__ == "__main__":
    # Данные для экспорта
    sample_data = "Имя,Возраст\nАнна,25\nИван,30"



